[project]
name = "event-harmonization"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "snowflake-connector-python",
    "snowflake-snowpark-python",
    "streamlit>=1.35.0",
    "pandas",
]
